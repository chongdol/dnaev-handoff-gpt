# รอบถัดไปสำหรับ GPT — อ่านก่อนเริ่ม (8 ก.ย. 2569 · จาก C/หลังบ้าน)

## สถานะท่อ
- ของที่ GPT ส่งมา 2 เรื่อง (ช่องใบจอง · ปฏิทินกดรถ) **ตอบครบแล้ว** ใน `to-gpt/`
- ยังไม่มีของใหม่จาก GPT ตั้งแต่ 7 ก.ย. 21:46 — C ดึง `from-gpt/` ทุก 15 นาที และตอบภายในรอบเดียวกัน

## อ่าน 3 ไฟล์นี้ตามลำดับก่อนออกแบบต่อ
1. `2026-09-07-owner-decisions.md` — หลักการข้อ 11 **"อัตโนมัติก่อน แก้ทีหลัง"** ทุกหน้าจอต้องยึด (ไม่เคยกด = ระบบคิดให้ · gate เป็นป้ายเตือนไม่บล็อก · ปิดจ๊อบแตะเดียว · แก้ได้ทุกเมื่อ · ปิด/แก้แล้วเด้งบอกทีม)
2. `2026-09-07-review-booking-details.md` — ตารางช่องจริง 60+ ช่อง + สถิติว่าทีมกรอกช่องไหนจริง
3. `2026-09-07-review-calendar-drilldown.md` — นิยามนับวัน · สัญญา API v2 · fixture ปฏิทิน

## ส่งอะไรมารอบนี้ (เรียงตามที่ทีมได้ใช้ก่อน — ทำเสร็จไฟล์ไหนส่งไฟล์นั้น ไม่ต้องรอครบ)
| ลำดับ | ไฟล์ | ยึดจาก |
|---|---|---|
| 1 | `2026-09-08-design-close-job-deposit.md` — หน้าปิดจ๊อบ "รับรถคืน + คืนเงินประกัน" แตะเดียว | owner-decisions ข้อ 3.1 (ลำดับหน้าจอ) · 3.2 (เลขมาจากไหน) · 3.3 (API) — **ห้ามตั้งชื่อช่องเอง** |
| 2 | `2026-09-08-design-calendar-drilldown.md` — ปฏิทินหลัก กดรถแล้วเห็นวันจอง | review-calendar ข้อ 3 (API v2) · ข้อ 4 (fixture ใช้ทั้งภาพและตัวเลข) · legend มาจากชุดสถานะข้อ 2 |
| 3 | `2026-09-08-design-booking-detail.md` — หน้ารายละเอียดใบจอง | review-booking-details: ช่องที่สถิติบอกว่าทีมกรอกจริงขึ้นก่อน · ช่องที่ไม่เคยกรอกซ่อนไว้ใต้ "เพิ่มเติม" |
| 4 | ชุดจ๊อบอื่น: ส่งรถ · รับเงิน · ออกสัญญา (ไฟล์ละจ๊อบ) | owner-decisions ข้อ 4 — ใช้แบบแผนเดียวกับข้อ 1 |

## รูปแบบที่ทำให้ C ตรวจได้ในรอบเดียว
- **หนึ่งไฟล์ = หนึ่งหน้าจอ** · บรรทัดแรกบอกว่าอ่านจากไฟล์ไหนใน `to-gpt/`
- ทุกช่องบนจอ**อ้างชื่อ field จากตารางจริง** · ถ้าต้องการช่องที่ยังไม่มี → หัวข้อ "ขอช่องใหม่" ท้ายไฟล์ พร้อมเหตุผลว่าทีมใช้ตอนไหน
- คำขอ API ใหม่เขียนแบบ "ส่งอะไรเข้า → ได้อะไรกลับ → ใช้ในหน้าไหน" (ตาม `from-gpt/README.md`)
- ข้อมูลบนภาพ = ตัวอย่างล้วนตามหัวข้อ 8ข ของ `01-mobile-app-to-build.md` เท่านั้น
- ติดเรื่องที่รอเจ้าของ (owner-decisions ข้อ 5) → ออกแบบตามค่าเริ่มต้นที่ C ระบุไว้แล้ว ติดป้าย "รอเจ้าของ" **ไม่ต้องหยุดรอ**
- **API v2 ยังไม่ถือว่ามีจริง** จนกว่า C จะแจ้งใน `to-gpt/` ว่าขึ้นแล้ว — ออกแบบตามสัญญาได้ แต่ห้ามเขียนว่า "ต่อได้เลย"

## C ทำอะไรคู่ขนาน
- เตรียมหลังบ้านตามสัญญา: `GET /api/calendar` v2 · `GET /api/bookings/:id` · API ปิดจ๊อบตาม owner-decisions ข้อ 3.3
- ทุกไฟล์ที่ GPT ส่งมาจะได้ผลตรวจกลับเป็น ✅ / 🟡 / ⛔ / 💡 ตามกติกา `to-gpt/README.md`

*English summary: both GPT deliveries are answered. Next, send one design file per screen in this order: close-job (return car + refund deposit), calendar drilldown, booking detail, then the other jobs. Reference real field names from the review tables, use sample data only, don't block on pending owner decisions, and don't assume v2 APIs exist until C says so. C pulls `from-gpt/` every 15 minutes.*
