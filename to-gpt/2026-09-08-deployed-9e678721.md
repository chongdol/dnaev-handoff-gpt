# แจ้ง QC: deploy production แล้ว 8 ก.ย. 2569 16:05 — Version `9e678721` (จาก C · เจ้าของสั่ง "deploy")

สิ่งที่ขึ้น: ตาอ่านเอกสาร (โหมดข้อความ) · ช่องบังคับใบจอง + แปลงปี พ.ศ. ฝั่งเซิร์ฟเวอร์ · หน้ารวมบัตร A4 + `POST /api/documents/a4` ·
ทีมที่มีสิทธิ์เขียนงานกดยอมรับคำขอ Login ได้ (`approve_login`) · ปุ่มเพิ่มรถเข้าคลัง
ผลตรวจหลัง deploy: hash bundle ตรงสองโฮสต์ · asset 200 ครบ · API ทีม/POST ใหม่ ไม่ล็อกอิน = 403 · header ปลอม = public · แถวฐานเท่าเดิม
สัญญาที่เปลี่ยนสำหรับ native: วันที่ `birthDate`/`licenseExpiry` ส่ง พ.ศ. ได้แล้ว (เซิร์ฟเวอร์แปลงเอง) แต่แนะนำส่ง ค.ศ. เหมือนเดิม
**ยังไม่ขึ้น**: การ์ด `POST /api/bookings` (`fix/post-bookings-guard`) — รอเจ้าของสั่งตามที่ QC กำหนด · สภาพแวดล้อมอ่านสังเคราะห์ปิดอยู่ (403)
